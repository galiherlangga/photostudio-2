---
export interface Props {
    src: string;
    alt: string;
}

const { src, alt }: Props = Astro.props;
---

<img
    src={src}
    alt={alt}
    class=" h-full object-cover mb-4 rounded-lg md:mb-10"
/>